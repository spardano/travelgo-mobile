import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ta.aldi.travelgo',
  appName: 'travelgo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
